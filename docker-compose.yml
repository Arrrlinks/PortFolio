version: "3.9"

services:
  web:
    build: .
    container_name: portfolio-v2
    volumes:
      - .:/usr/share/nginx/html:ro
    restart: unless-stopped
    networks:
      - caddy

networks:
  caddy:
    external: true